# TL;DR
A collection of library routines I've written myself for my own use. C/C++ and Python for now. Rust and Go might join soon.

Feel free to use. I am not responsible for the consequences.

# Available Components
 * A simple logger for `C` and `C++`. For `Go`, `maxbolgarin/logze` library is working just fine. `Python` has excellent logging built-in. `Rust` pending.
 * A simple CLI parser for `C++`. Manual parsing for `C`. `Python` has excellent CLI arguments parser built in. `Go` and `Rust` pending.
 * Some queue and binary tree implementations in 'C', primarily for IoT platforms. For `C++`, `Go` and `Python` got built-in primitives performing really well in most cases. `Rust` pending.
 * Simple logger setup routines for Python.

# License
GPL v2. All warranties disclaimed.

# Found an issue
Please feel free to open an issue. Will see if I can get around to fix it.

# Contributing
Send a PR, please. Might take a while for me to work on it.

# Motivation
For my personal use mostly. Occasionally to scratch an itch. Just to keep myself sharp sometimes.

Of course, it is always a goal to improve the build speed, run-time efficiency and/or binary size etc.

# Why not use `xxxxxxx` or `yyyyyy`?
Mostly because I did not know about it. Or I wanted a better metric or licensing or just to implement it my own way or even to pratice. Or some combinations of all of the above. See [Motivation](#Motivation) above, please.

